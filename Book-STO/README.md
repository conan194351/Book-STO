# Book-STO

---------------------------
Using go version 1.22

```shell
docker compose up -d
```
Copy sql file from database run in mysql

```shell
cp .env.example .env
```

```shell
go run main.go
```